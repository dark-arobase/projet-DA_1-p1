import tkinter as tk
import re

root = tk.Tk()
root.title("Menu principal")
root.geometry("400x400")

header_font = ("Arial", 36, "bold")

label = tk.Label(root, text = "Veuillez choisir un quiz ", font= header_font, background="black", fg="white",  )
label.pack() 








root.mainloop()